# Sideband Bridge Configuration
# Copy this file to .env.local and update the values

# For development (local)
NEXT_PUBLIC_SIDEBAND_HOST=localhost
NEXT_PUBLIC_SIDEBAND_PORT=8888  # Default port for netlify dev
NEXT_PUBLIC_SIDEBAND_HASH=your-sideband-hash

# For production (set these in Netlify environment variables)
# NEXT_PUBLIC_SIDEBAND_HOST=your-site.netlify.app
# NEXT_PUBLIC_SIDEBAND_PORT=443
# NEXT_PUBLIC_SIDEBAND_HASH=your-production-hash

# GitHub OAuth for Netlify CMS (if used)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
